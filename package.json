{
  "name": "flickfive",
  "version": "1.0.0",
  "private": true,
  "description": "Pick 5 movies. Fit 5 daily rules. Chase the highest score.",
  "scripts": {
    "start": "node server.js",
    "generate": "node scripts/generate-movies.js",
    "export": "node scripts/export-static.js",
    "build": "npm run export"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2"
  }
}
